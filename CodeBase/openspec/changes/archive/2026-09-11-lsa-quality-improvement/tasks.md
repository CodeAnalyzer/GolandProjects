## 1. Benchmark-фикстура (этап 0)

- [x] 1.1 Создать синтетический мини-корпус спек в testdata (≥120 capability для прохода `LSAMinCorpus=100`): заложить ко-встречаемость «арест»↔«блокировка», редкий термин «заметка» в текстах требований (df ≥ 3), высокочастотный «клиент» как источник шума. Верификация: файлы фикстуры существуют и парсятся openspec-парсером
- [x] 1.2 Написать golden-тест обучения LSA на синтетике: словарь (состав терминов), состав документов (title×2, req/scn тексты), косинусы контрольных запросов. Тест сознательно красный до реализации этапов 2–5. Верификация: `go test ./internal/specfts/... ./internal/indexer/...` с golden-файлами
- [x] 1.3 Замерить стоимость полного переобучения на боевом корпусе FA (загрузка корпуса → SVD → перезапись vocab/embeddings). Верификация: время зафиксировано; при >30 сек — решение о ненулевом дефолте `lsa_retrain_threshold` (контракт не меняется)

## 2. Детерминированная загрузка корпуса

- [x] 2.1 В `internal/store/db_lsa.go` добавить `LoadAllSpecCapabilitiesWithReqsForLSA`: два коррелированных подзапроса `string_agg(... ORDER BY req_order/scn_order, id)` + `concat_ws` для NULL-безопасности; в `model.SpecCapability` добавить поле `LSAText string`. Верификация: тест агрегации — полнота (все req/scn), порядок, NULL-тексты не ломают результат
- [x] 2.2 В `internal/indexer/indexer_postprocess_spec_lsa.go` собрать документ `title×2 + purpose + notes + LSAText` (строки 59 и 129 — обучение и embed_text). Верификация: unit-тест состава документа — title встречается дважды, тексты требований и сценариев включены

## 3. Fingerprint и state переобучения

- [x] 3.1 Ввести константы `compositionVersion`, `tokenizerVersion` в `internal/specfts`; `compositionVersion` поднять (состав меняется этим change). Верификация: компиляция, константы используются в fingerprint
- [x] 3.2 Реализовать `CorpusFingerprint(caps []model.SpecCapability, params LSAParams) string` — SHA-256 потоково по текстам (\x00-разделители), параметрам (min_df, max_df, k) и версиям. Верификация: unit-тесты — детерминизм при повторе, чувствительность к тексту требования, к параметру, к версии токенизатора
- [x] 3.3 Реализовать sidecar `spec_lsa_state.json` рядом с моделью (`{fingerprint, pending, params, num_docs, trained_at}`), атомарная запись tmp+rename, функции load/save в `internal/specfts`. Верификация: unit-тесты save/load/повреждённый файл
- [x] 3.4 Переписать логику `postProcessSpecLSA` на машину состояний из design D2: быстрый путь (0 изменений и параметры совпали), идемпотентный пропуск (fingerprint совпал), кумулятивный `pending` + порог, при ошибке обучения state не меняется. Верификация: тесты — первый прогон обучает, повторный без изменений пропускает, изменения ниже порога копятся, накопление через прогоны переобучает, ошибка SVD сохраняет state
- [x] 3.5 Обеспечить Δ_прогона = изменённые сущности (capability + requirement + scenario) включая удаления: проверить, считает ли stats удаления spec-сущностей; при отсутствии — инкрементировать счётчик в очистке устаревших файлов по числу сущностей удаляемого файла. Верификация: тест — удаление spec-файла увеличивает pending и (при пороге 0) переобучает
- [x] 3.6 Конфиг `internal/config/config.go`: `LSARetrainThreshold` default 0 (0 легален = «при любом изменении»), убрать затирающий дефолт `<=0 → 50`. Верификация: тест `ensureDefaults` — явный 0 сохраняется

## 4. Нормализация ё→е

- [x] 4.1 В `internal/specfts/tokenizer.go` нормализовать ё→е перед стеммингом, поднять `tokenizerVersion`. Верификация: тесты — «счёт»/«счет» дают один стем, «счёта»/«счета» совпадают, существующие категории токенов не затронуты

## 5. Фильтрация semantic-секции

- [x] 5.1 Реализовать чистую функцию `filterSemanticHits(hits, minCosine, relativeCutoff, limit)` с порядком: абсолютный порог → сортировка DESC → relative cutoff (× maxRank) → limit. Верификация: табличный unit-тест без БД — граничные случаи (пустой список, cutoff отсекает до limit, все ниже порога)
- [x] 5.2 Конфиг: `LSAMinCosine` (default 0.15), `LSARelativeCutoff` (default 0.5) с валидацией диапазона [0,1] — некорректные значения дают ошибку загрузки. Верификация: тесты валидации и defaults
- [x] 5.3 Интегрировать `filterSemanticHits` в `searchSpecSemantic` (`internal/specsvc/specsvc.go`) вместо фильтра 0.01. Верификация: существующие тесты specsvc зелёные, поведение порогов покрыто тестом через конфиг

## 6. Диагностика semantic-секции

- [x] 6.1 OOV-диагностика: стемы запроса вне словаря LSA попадают в ответ (hint со списком OOV-стемов и отсылкой к exact-слою). Верификация: тест — запрос с OOV-термином возвращает hint
- [x] 6.2 Метаполе пустой semantic-секции: `{filtered_out, threshold, reason}` при пустоте после фильтрации — в MCP-ответе и CLI JSON. Верификация: тест структуры ответа

## 7. minDF-аудит (гейт по данным)

- [x] 7.1 Замерить на боевом корпусе FA: df(«заметк»), размер словаря и долю OOV-терминов при `minDF ∈ {1, 2, 3}`. Верификация: результаты зафиксированы, значение выбрано
- [x] 7.2 Применить выбранное значение (default в `ensureDefaults`), при изменении — обновить golden-файлы и конфиг-документацию. Верификация: golden-тесты зелёные

## 8. Сквозная верификация

- [x] 8.1 Полная сборка и тесты: `go build ./...` и `go test ./...` без падений
- [x] 8.2 `codebase init` на боевом корпусе FA + ручной чек-лист контрольных запросов («Заметка по клиенту», «блокировка счёта», «лимит карточки», «обработка платежного документа», «параметры операции», техимена) с фильтром продукта и без: «Заметка по клиенту» semantic — `legal` в топе, шум 0.4x отсечён. Верификация: заполненный чек-лист
- [x] 8.3 `openspec validate lsa-quality-improvement` зелёный; спека `query/spec-search` соответствует поведению
