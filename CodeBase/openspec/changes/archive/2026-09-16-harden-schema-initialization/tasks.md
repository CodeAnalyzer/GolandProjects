## 1. Конкурентно-безопасная инициализация схемы

- [x] 1.1 Добавить PostgreSQL integration-тест с несколькими независимыми DB handles, одновременно вызывающими `InitSchemaCtx` для одной пустой БД; проверить, что до реализации тест воспроизводит конфликт, а после неё все вызовы успешны и обязательные объекты созданы.
- [x] 1.2 Перевести полный проход `InitSchemaCtx` на транзакцию выделенного `sql.Conn` с `pg_advisory_xact_lock` до первого DDL, адаптировать одноразовую migration к общей транзакции и проверить существующие тесты идемпотентности/миграций `internal/store`.
- [x] 1.3 Добавить integration-тест отмены context во время ожидания занятого schema-init lock и проверить, что вызов своевременно возвращает ошибку context, а последующий вызов получает lock без утечки.

## 2. Версия и read-only проверка схемы

- [x] 2.1 Ввести непрозрачный текущий schema-marker и записывать его в `schema_migrations` только в успешной транзакции `InitSchemaCtx`; проверить тестами наличие маркера после первого и повторного прохода и его отсутствие после rollback.
- [x] 2.2 Реализовать read-only checker с классифицированными ошибками для отсутствующей таблицы миграций, отсутствующего текущего маркера и прочей ошибки БД; проверить integration-тестами пустую, устаревшую и актуальную схемы, включая неизменность каталога/данных после проверки.
- [x] 2.3 Добавить безопасное формирование диагностики с ожидаемым и последним обнаруженным schema-marker без DSN/пароля и проверить точные категории ошибок и отсутствие секретов unit-тестами.

## 3. Read-only MCP startup

- [x] 3.1 Выделить тестируемый этап подготовки MCP до запуска stdio и заменить вызов `InitSchema` на read-only checker; проверить тестом, что при актуальной схеме registry/transport подготавливается без DDL.
- [x] 3.2 Добавить тесты MCP startup для отсутствующей и устаревшей схемы; проверить завершение до обслуживания tools, рекомендации `codebase init`/`codebase update` и отсутствие автоматически созданных объектов.
- [x] 3.3 Добавить subprocess/integration-тест одновременного запуска профилей `query`, `rti`, `trc` и `review` на актуальной тестовой схеме; проверить успешный initialize/tools-list каждого процесса и отсутствие `pg_class_relname_nsp_index`/`Connection closed`.

## 4. Регрессия и эксплуатационная проверка

- [x] 4.1 Запустить `go test ./internal/store ./internal/mcp ./cmd` и исправить все регрессии, считая задачу выполненной только при успешном результате.
- [x] 4.2 Запустить `go test ./...` и `go build ./...`; проверить, что полный набор тестов и сборка завершаются без ошибок.
- [x] 4.3 На временной тестовой БД проверить миграционный путь «старая схема → `codebase update` → текущий marker → параллельный MCP startup», не изменяя рабочий индекс; зафиксировать успешный повторный запуск всех четырёх профилей.
